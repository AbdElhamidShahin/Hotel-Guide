
import 'package:flutter/material.dart';

class CasesBookingProcess extends StatelessWidget {
  const CasesBookingProcess({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
